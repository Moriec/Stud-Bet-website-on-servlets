package com.studbet.util.validate;

public interface UserValidate {
    boolean isUsernameValid(String username);
    boolean isEmailValid(String username);
    boolean isPasswordValid(String password);
}
